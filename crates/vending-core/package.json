{
  "name": "vending-core",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "cargo build -p vending-core",
    "fmt": "cargo fmt -p vending-core",
    "lint": "cargo clippy -p vending-core --all-targets",
    "test": "cargo test -p vending-core --all-targets",
    "typecheck": "cargo check -p vending-core --all-targets"
  }
}
