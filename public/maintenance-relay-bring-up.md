# Maintenance Relay Historical Tombstone

This document is historical only. It does not define an active deployment,
bring-up, maintenance, or runtime acceptance path.

Windows runtime stabilization uses direct certificate-only SSH deployment to a
clean prepared host. Follow the active Windows runtime delivery and acceptance
documents instead of recreating any relay-based maintenance path.
