# Historical Vision Release Bundle Runbook

This document describes a retired release-governance path and is retained only
as a tombstone. It is not an active build, installation, deployment, or
acceptance procedure.

Current Windows runtime work consumes the Vision artifact through Runtime
Bootstrap and the current stabilization path in ADR 0075. Deploy it directly
over certificate-only SSH with its site configuration, then verify the Vision
process as an independent runtime capability.
